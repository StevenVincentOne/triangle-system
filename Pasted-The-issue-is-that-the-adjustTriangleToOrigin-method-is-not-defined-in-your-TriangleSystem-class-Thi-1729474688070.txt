The issue is that the adjustTriangleToOrigin method is not defined in your TriangleSystem class. This method is being called in the initializeSystem method, but it doesn't exist. Let's fix this by adding the missing method to your TriangleSystem class in the script.js file.

Add the following method to your TriangleSystem class:

adjustTriangleToOrigin() {
    const centerX = (this.system.n1.x + this.system.n2.x + this.system.n3.x) / 3;
    const centerY = (this.system.n1.y + this.system.n2.y + this.system.n3.y) / 3;
    this.system.n1.x -= centerX;
    this.system.n1.y -= centerY;
    this.system.n2.x -= centerX;
    this.system.n2.y -= centerY;
    this.system.n3.x -= centerX;
    this.system.n3.y -= centerY;
}
This method calculates the centroid of the triangle and then adjusts all vertices so that the centroid is at the origin (0, 0).

Now, make sure your initializeSystem method looks like this:

initializeSystem(preset = 'equilateral') {
    const side = 300;
    const height = (Math.sqrt(3) / 2) * side;
    switch (preset) {
        case 'equilateral':
            this.system = {
                n1: { x: 0, y: height },
                n2: { x: -side / 2, y: 0 },
                n3: { x: side / 2, y: 0 },
            };
            break;
        // ... other cases ...
    }
    this.adjustTriangleToOrigin();
    this.updateDerivedPoints();
    this.updateDashboard();
    this.drawSystem();
}
Also, ensure that the updateDerivedPoints method is defined in your class. If it's not, add a placeholder method:

updateDerivedPoints() {
    // Calculate and update any derived points here
    // For example, calculate the incenter
    this.calculateIncenter();
}
calculateIncenter() {
    // Calculate the incenter of the triangle
    const a = this.calculateDistance(this.system.n2, this.system.n3);
    const b = this.calculateDistance(this.system.n1, this.system.n3);
    const c = this.calculateDistance(this.system.n1, this.system.n2);
    const x = (a * this.system.n1.x + b * this.system.n2.x + c * this.system.n3.x) / (a + b + c);
    const y = (a * this.system.n1.y + b * this.system.n2.y + c * this.system.n3.y) / (a + b + c);
    this.system.incenter = { x, y };
}
These changes should resolve the "this.adjustTriangleToOrigin is not a function" error and allow your triangle system to initialize properly. After making these changes, the triangle should display and the data should populate in your application.